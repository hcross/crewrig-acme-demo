# Crewrig fork configuration — ACME Corp overlay.
#
# Generated for the CrewRig training fork. This file is overlay (owned by
# ACME Corp) and is never overwritten by `scripts/sync-from-upstream.sh`.

# canonical_repo — kept pointing at the upstream we forked from, so the
# audit trail and license trace remain intact (we are NOT the canonical
# upstream).
canonical_repo = "https://github.com/crewrig/crewrig"

# feedback_repo — this fork's own tracker. Friction MRs on adopter-owned
# tiers (artifacts/community, artifacts/org, extensions/org) land here.
# Components we did not author still route upstream by design.
feedback_repo  = "https://github.com/hcross/crewrig-acme-demo"
