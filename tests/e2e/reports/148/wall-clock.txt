Check 2 — wall-clock for the gemini -p path under the new /run/gemini-creds bundle pattern.

NOTE: The full `task e2e:test` run could NOT measure scenario-level wall-clock
for `gemini/01-layered-context` because the scenario script
`tests/e2e/scenarios/01-layered-context/run.sh` double-mounts the bundle
(see Defect note below); the bootstrap `cp -a` aborts before `gemini -p`
is invoked. Failure is immediate, NOT a hang — so the pre-#148 EXIT=124
silent timeout is already eliminated as a class. Verified against the
report directory:
  tests/e2e/reports/20260530T154155Z-29a6/gemini/01-layered-context/

To isolate the gemini-p wall-clock under the new pattern, the equivalent
single-mount docker invocation from Check 3 was timed instead:

  docker run --rm -v ~/.crewrig-e2e/gemini:/run/gemini-creds:ro \
    crewrig/e2e-gemini:latest bash -c '
      set -e
      mkdir -p /home/agent/.gemini
      cp -a /run/gemini-creds/. /home/agent/.gemini/
      chown -R agent:agent /home/agent/.gemini 2>/dev/null || :
      gemini -p "one colour. one word."
    '

Result: end-to-end wall-clock 10.698 s (zsh `time` builtin),
including docker startup, image pull cache, bootstrap `cp -a`, and the
gemini -p round-trip.

Decision 7 acceptance:
  < 10 s   → PASS
  10 – 60 s → PARTIAL
  > 60 s   → FAIL

Verdict: PARTIAL — 10.698 s sits just outside the < 10 s target. The
overshoot is bounded by docker-run startup overhead (~1–2 s on Docker
Desktop macOS) and the bootstrap copy of the ~25-file bundle; the
gemini -p phase itself completes in single-digit seconds (consistent
with #147 §4.1 healthy host < 5 s). The 120 s timeout class is
empirically eliminated.

Defect blocking a clean scenario-level measurement is reported to
team-lead; once fixed, re-run will yield the runner-level wall-clock
under Decision 7's measurement contract.

---

## Re-run at commit 047260f (Decision 5 Revision 2)

The scenario-script double-mount was fixed (case-split skips the
redundant rules mount for gemini). A NEW mount line was added to
`tests/e2e/defaults.toml [cli.gemini].mounts`:

  "${HOME}/.gemini:/run/gemini-rules:ro"

The runner's token map (`tests/e2e/run.sh:178–179`) was extended to
interpolate `${HOME}` in addition to `${CREWRIG_E2E_HOME}`. However,
the scenario script `tests/e2e/scenarios/01-layered-context/run.sh:75`
carries its OWN local `expand_mount` that only handles
`${CREWRIG_E2E_HOME}` — the `${HOME}` token is NOT interpolated when
the scenario script processes mounts from the effective config.

Docker rejects the literal `${HOME}/.gemini:/run/gemini-rules:ro` with:

  docker: Error response from daemon: create ${HOME}/.gemini:
  "${HOME}/.gemini" includes invalid characters for a local volume
  name, only "[a-zA-Z0-9][a-zA-Z0-9_.-]" are allowed.

Scenario-level wall-clock still unmeasurable; failure is immediate, not
a hang. The 120 s timeout class remains empirically eliminated.

Report dir: tests/e2e/reports/20260530T163700Z-7a03/gemini/01-layered-context/

Defect reported to team-lead — same scenario-script file as the prior
round, different forgotten edit.

---

## Re-run at commit 6fcac29 (expand_mount factored into shared lib)

`task e2e:test -- --scenario 01-layered-context --cli gemini`. The
`${HOME}` token now interpolates correctly: docker invocation shows
`-v /Users/hoanicross/.gemini:/run/gemini-rules:ro`. Container
bootstrap runs and copies all seven layered-context rules into
`/home/agent/.gemini/` (verified by an isolated bootstrap repro:
00_SOUL.md, 10_USER_LEVEL.md, 20_ORGANIZATION.md, 30_USER_PROFILE.md,
40_USER_EXPERTISE.md, 50_USER_TEAM.md, 60_TOOLS.md). `gemini -p`
launches and answers.

Scenario-level wall-clock: extractable now. The targeted run took
~docker-startup + bootstrap + gemini -p; full scenario wall-clock
from runner perspective < 15 s. Under Decision 7 the 120 s timeout
class stays gone.

Outcome: `out/answer.txt` = "Zurich" — model hallucinated. Acceptance
`[Nn]antes` not met. probe.stderr shows the model attempted to read
`/home/agent/.gemini` directly and was blocked by the gemini CLI's
workspace allowlist (`Path not in workspace: Attempted path
"/home/agent/.gemini" resolves outside the allowed workspace
directories: /home/agent/workspace`). The container-side bundle is
correct; the **context-loading wiring** is missing — settings.json
contains only `security.auth.selectedType` and no `context` block,
and there is no `GEMINI.md` entry point at `~/.gemini/` or in
`/home/agent/workspace`. Without one of those, gemini does not load
the numbered rule files as in-context.

Report dir: tests/e2e/reports/20260530T164640Z-1fbe/gemini/01-layered-context/

Defect surfaced to team-lead. NOT in tester scope to wire the
context-loading mechanism.
